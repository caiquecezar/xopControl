import {StyleSheet} from 'react-native'

const estilo = StyleSheet.create({
    logo: {
      width: 40,
      height: 40,
      margin: 15,
    },
    editar: {
      width: 20,
      height: 20,
    },
    tituloTexto: {
      fontSize: 15,
      textAlign: 'left',
    }
  })

export default estilo;